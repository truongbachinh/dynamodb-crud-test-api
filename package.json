{
  "name": "dynamodb-crud-test-api",
  "version": "1.0.0",
  "description": "DynamoDB CRUD API using API Gateway and AWS Lambda",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.200.0",
    "@aws-sdk/util-dynamodb": "^3.200.0",
    "aws-sdk": "^2.1244.0"
  },
  "devDependencies": {
    "serverless-iam-roles-per-function": "^3.2.0"
  }
}
